import heap

# Create an empty list to store the heap elements
heap = []

# Add elements to the heap
heap.insert(heap, 5)
heap.insert(heap, 2)
heap.insert(heap, 10)
heap.insert(heap, 1)

# Print the heap
print(heap)